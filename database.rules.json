{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "$other": {
      ".read": "auth !== null",
      ".write": "auth !== null"
    }
  }
}
